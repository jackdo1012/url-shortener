<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta http-equiv="X-UA-Compatible" content="IE=edge" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>URL Shortener</title>
        <link
            href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css"
            rel="stylesheet"
            integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3"
            crossorigin="anonymous"
        />
    </head>
    <style>
        .new_url {
            position: relative;
            text-align: center;
            width: 50vw;
            left: 25vw;
            padding: 20px;
        }

        table {
            width: 50vw;
            border-collapse: collapse;
            margin-left: 50vw;
            transform: translateX(-50%);
        }

        table tr * {
            border: 2px solid black;
        }

        table tbody tr td,
        table thead tr th {
            text-align: center;
            padding: 0.5rem;
        }
    </style>
    <body>
        <h1 class="text-center m-5">URL Shortener</h1>
        <form class="new_url" action="/add" method="POST">
            <input
                class="form-control"
                type="text"
                placeholder="Enter slug"
                name="slug"
            />
            <input
                class="form-control"
                type="text"
                placeholder="Enter URL"
                name="link"
            />
            <button class="btn btn-primary" type="submit">Submit</button>
        </form>
        <table>
            <table>
                <thead>
                    <tr>
                        <th>Slug</th>
                        <th>Link</th>
                    </tr>
                </thead>
                <tbody>
                    <% data.forEach(function(small) { %>
                    <tr>
                        <td><%= small.slug %></td>
                        <td><%= small.link %></td>
                    </tr>
                    <% }) %>
                </tbody>
            </table>
        </table>
    </body>
</html>
